{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([jis_standards.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'jis_standards.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
